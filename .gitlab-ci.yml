
build:

  image: ubuntu:devel
  stage: build

  script:  

    # Update and install
    - apt update
    - apt full-upgrade --yes
    - apt install --yes neofetch make cmake clang

    # Version check
    - neofetch
    - make --version
    - cmake --version
    - clang --version

    # Build and run
    - make
