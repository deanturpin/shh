{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "shh (lldb)",
            "type": "cppdbg",
            "request": "launch",
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/build/shh",
            "MIMode": "lldb",
        }
    ]
}